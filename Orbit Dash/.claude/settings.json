{
  "permissions": {
    "defaultMode": "plan",
    "deny": [
      "Write(app/**)",
      "Edit(app/**)"
    ],
    "allow": [
      "Bash(./gradlew assembleDebug)",
      "Bash(./gradlew installDebug)"
    ]
  }
}
