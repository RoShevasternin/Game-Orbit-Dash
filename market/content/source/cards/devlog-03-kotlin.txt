// design/ball_magnet.svg → one baked texture
val waveTex = VfxTexture(
    115f, 115f,
    shape = RadialGradientEffect()
        .stop(0.745f, magnet, 0.25f)
        .stop(0.90f,  magnet, 0.70f)
        .stop(1f,     magnet, 0f),
    density = 0.5f,
)

// four copies, phase-shifted, contracting
for (w in aWaves) add(w) { size(115f); center() }
